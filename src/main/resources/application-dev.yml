spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bridge
    username: bridge
    password: ${DB_PASSWORD:bridge_dev}

app:
  operador:
    email: ${OPERADOR_EMAIL:admin@bridge.local}
    password: ${OPERADOR_PASSWORD:cambiar123}

arca:
  cuit-emisor: "20463447277"
  punto-venta: 1
  certificado: ${ARCA_CERT_PATH}
  clave-privada: ${ARCA_KEY_PATH}
  url-wsaa: https://wsaahomo.afip.gov.ar/ws/services/LoginCms
  url-wsfe: https://wswhomo.afip.gov.ar/wsfev1/service.asmx
  ambiente: HOMOLOGACION
  timeout-conexion-segundos: 15
  timeout-respuesta-segundos: 45
